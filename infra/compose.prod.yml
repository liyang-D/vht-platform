services:
  postgres:
    restart: unless-stopped

  orchestrator:
    restart: unless-stopped

  llm:
    restart: unless-stopped

  simple-chat-backend:
    restart: unless-stopped

  simple-chat-frontend:
    restart: unless-stopped

  gateway:
    ports:
      - "127.0.0.1:${GATEWAY_PORT:-8088}:80"
    restart: unless-stopped
